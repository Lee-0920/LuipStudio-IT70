status.remain =
{
    standard = config.remain.standard.total,
    blank = config.remain.blank.total,
    zeroCheck = config.remain.zeroCheck.total,
    rangeCheck = config.remain.rangeCheck.total,
    waste = config.remain.waste.total,
    wasteWater = config.remain.wasteWater.total,
    reagent1 = config.remain.reagent1.total,
    reagent2 = config.remain.reagent2.total,
}
